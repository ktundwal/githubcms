@{
    ViewBag.Title = "Emojis";
}
@using Octokit
@model IEnumerable<Emoji>

<h1>Emojis</h1>

<div>
    @foreach (var emoji in Model)
    {
        <span><img src="@emoji.Url" /> @emoji.Name</span>
    }
</div>